int counteq(short* v1, short* v2, int n) {
    int c = 0, 
    int a = 0;
    short* b = v1;
    short* di = v2;
L:
	if (i >= n) goto E;
	int d = v1[i];
	int 
    for (i=0; i<n; ++i) cnt += (v1[i]==v2[i]);
    return cnt;
}
